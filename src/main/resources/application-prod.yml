server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://prod-db:5432/delivery_db
    username: prod_user
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true

management:
  endpoint:
    health:
      show-details: never